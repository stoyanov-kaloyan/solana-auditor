# Findings

This directory stores:

- Reports generated by `/solana-auditor` runs (`{project-name}-solana-ai-audit-report-{timestamp}.md`)
- External/manual audit reports you want re-checked against current code

On each run, the skill may read these files to identify previously reported issues and highlight which ones still appear present.
